
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - BookHive</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
</head>
<h2>Login</h2>
<body>
    <div class="container">
        <header>
            <h1>{{title}}</h1>
            <p>{{description}}</p>
        </header>
        <main>
    <form action="{{loginUrl}}" method="post" class="login-form">
          <div class="mb-3">
            <label for="email" class="form-label form-label-black">Username</label>
            <input type="email" class="form-control" id="email" name="email" placeholder="Enter your email" required>
        </div>
        <div class="mb-3">
            <label for="email" class="form-label form-label-black">Email address</label>
            <input type="email" class="form-control" id="email" name="email" placeholder="Enter your email" required>
        </div>
        <div class="mb-3">
            <label for="password" class="form-label form-label-black">Password</label>
            <input type="password" class="form-control" id="password" name="password" placeholder="Enter your password" required>
        </div>
        <div class="d-flex">
                <!-- Login button -->
                <form action="{{loginUrl}}" method="post" class="me-2">
                    <button type="submit" class="btn btn-secondary">Login</button>
                </form>

                <!-- Sign Up button -->
                <form action="{{signupUrl}}" method="get">
                    <button type="submit" class="btn btn-secondary">Sign Up</button>
                </form>
        </main>
    </div>
</body>
</html>
